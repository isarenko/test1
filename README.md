СПП Service (Backend)
Описание проекта

Прототип backend-сервиса для управления иерархическим справочником СПП и выполнения каскадного распределения суммы по древовидной структуре.

Функциональность
- получение дерева СПП с фильтрацией по датам (версионность)
- распределение суммы по выбранным узлам
- рекурсивный расчет значений по дереву
- сохранение результатов расчётов
- хранение временных результатов в Redis
- перенос результатов в PostgreSQL
- экспорт результатов в Excel
- SSE-уведомления о событиях

Технологический стек
- Python 3.10
- FastAPI
- PostgreSQL 16
- Redis 7
- Docker / Docker Compose
- openpyxl

Архитектура
Сервис построен по принципу разделения ответственности:
- PostgreSQL — источник данных (source of truth)
- Redis — временное хранилище расчетов
- API — слой вычислений и агрегации
- SSE — канал real-time уведомлений

Поток данных
1. /calculate
загрузка дерева из PostgreSQL
выполнение алгоритма распределения
сохранение результата в Redis (TTL 3600)

2. /save/{calc_id}
чтение результата из Redis
запись в PostgreSQL (JSONB)

3. /stream/{session_id}
подписка на события сохранения (SSE)

Модель данных
- spp_elements
Иерархический справочник:
parent_id — структура дерева (adjacency list)
valid_from / valid_to — версионность
is_active — активность записи

- calculations
Результаты расчетов:
calc_id (UUID)
session_id
status
data (JSONB snapshot)
created_at

- departments / department_spp_links
Справочники отделов (в рамках ТЗ, не используются в расчётах)

Алгоритм распределения
Используется DFS (обход в глубину):
выбранные узлы делят сумму поровну
каждый узел рекурсивно распределяет свою долю вниз
листья получают финальные значения
родитель агрегирует сумму детей
Округление: 2 знака после запятой.

Запуск проекта
Требования - Docker
Docker Compose

Запуск
docker compose up --build

API
Swagger: http://localhost:8000/docs
GET /tree
POST /calculate
POST /save/{calc_id}
GET /calculations
GET /calculations/{calc_id}
GET /export/{calc_id}
GET /stream/{session_id}

Redis → PostgreSQL → Real-time

Redis
Используется как временное хранилище:
- результаты расчётов
- TTL 3600 секунд
- быстрый доступ до сохранения

PostgreSQL
Основное хранилище:
- справочник СПП
- сохранённые расчёты
- JSONB snapshot результата

Real-time (SSE)
- подписка на канал calc_updates
- уведомления о сохранении расчётов
- упрощённая альтернатива WebSockets

Принятые архитектурные решения
- adjacency list (parent_id) для дерева
- Redis как промежуточный слой между расчетом и БД
- синхронный расчет (без очередей, MVP уровень)
- SSE вместо WebSocket
- хранение результата в JSONB
- Docker Compose как единая точка запуска

Тестирование
Проверки выполнялись вручную через Swagger:
- корректность дерева /tree
- фильтрация по датам (valid_from / valid_to)
- распределение суммы
- Redis TTL
- сохранение в PostgreSQL
- экспорт Excel
- SSE поток

Использование ИИ (AI workflow)
Использовались LLM-инструменты для ускорения разработки:
Задачи
- проектирование PostgreSQL схемы
- разработка алгоритма DFS распределения
- настройка Docker Compose
- генерация FastAPI endpoints
- отладка Docker/PostgreSQL
- настройка Redis слоя
Примеры промтов
"Реализуй рекурсивное распределение суммы по дереву в Python"
"Спроектируй PostgreSQL схему с версионностью"
"FastAPI + Redis + PostgreSQL архитектура"
"Docker compose multi-service setup"

Инженерный контроль
Сгенерированный код был проверен:
- ручное тестирование через Swagger
- проверка корректности расчетов
- анализ Docker логов
- исправление ошибок init.sql и volumes
- проверка Redis → PostgreSQL консистентности
- тестирование edge cases
